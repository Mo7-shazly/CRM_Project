<UserControl x:Class="CRM.Desktop.Views.QuotationsView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
<Grid Margin="28,25"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
<Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions><StackPanel><TextBlock Text="Quotations" FontSize="28" FontWeight="Bold" Foreground="#18283C"/><TextBlock Text="Create, manage and export customer quotations" Foreground="#77849A"/></StackPanel><Button Grid.Column="1" Content="New Quotation" Command="{Binding NewQuotationCommand}" Background="#1677C8" Foreground="White" Padding="16,9" BorderThickness="0"/></Grid>
<DataGrid Grid.Row="1" Margin="0,20,0,0" ItemsSource="{Binding Quotations}" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"><DataGrid.Columns><DataGridTextColumn Header="Quote #" Binding="{Binding QuoteNumber}" Width="95"/><DataGridTextColumn Header="Customer" Binding="{Binding Customer.CompanyName}" Width="*"/><DataGridTextColumn Header="Issue date" Binding="{Binding IssueDate, StringFormat={}{0:d}}" Width="120"/><DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="110"/><DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat={}{0:C0}}" Width="110"/><DataGridTemplateColumn Header="Actions" Width="150"><DataGridTemplateColumn.CellTemplate><DataTemplate><StackPanel Orientation="Horizontal"><Button Content="Edit" Command="{Binding DataContext.EditQuotationCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/><Button Content="PDF" Command="{Binding DataContext.ExportPdfCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" Margin="6,0"/></StackPanel></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn></DataGrid.Columns></DataGrid>
<Grid Visibility="{Binding IsEditorOpen, Converter={StaticResource BoolToVisibility}}" Grid.RowSpan="2" Background="#99000000"><Border Width="760" Padding="28" Background="White" HorizontalAlignment="Center" VerticalAlignment="Center"><Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><TextBlock Text="Quotation details" FontSize="23" FontWeight="Bold"/><TextBlock Grid.Row="1" Text="{Binding EditorError}" Foreground="#C83D3D" Margin="0,5"/><Grid Grid.Row="2" Margin="0,12,0,0"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions><Grid><Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions><ComboBox ItemsSource="{Binding Customers}" DisplayMemberPath="CompanyName" SelectedValuePath="Id" SelectedValue="{Binding Editor.CustomerId}" ToolTip="Customer *" Margin="0,0,8,8"/><ComboBox Grid.Column="1" ItemsSource="{Binding Statuses}" SelectedItem="{Binding Editor.Status}" Margin="0,0,8,8"/><DatePicker Grid.Column="2" SelectedDate="{Binding Editor.ValidUntil}" ToolTip="Valid until" Margin="0,0,0,8"/></Grid><DataGrid Grid.Row="1" ItemsSource="{Binding Editor.Items}" AutoGenerateColumns="False" CanUserAddRows="False"><DataGrid.Columns><DataGridTextColumn Header="Description" Binding="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Width="*"/><DataGridTextColumn Header="Qty" Binding="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Width="90"/><DataGridTextColumn Header="Unit price" Binding="{Binding UnitPrice, UpdateSourceTrigger=PropertyChanged}" Width="110"/><DataGridTemplateColumn Header=""><DataGridTemplateColumn.CellTemplate><DataTemplate><Button Content="Remove" Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}"/></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn></DataGrid.Columns></DataGrid></Grid><StackPanel Grid.Row="3"><Button Content="Add item" Command="{Binding AddItemCommand}" HorizontalAlignment="Left"/><TextBox Text="{Binding Editor.Notes}" ToolTip="Notes" Height="52" AcceptsReturn="True" Margin="0,10,0,0"/><StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0"><Button Content="Cancel" Command="{Binding CancelEditCommand}"/><Button Content="Save quotation" Command="{Binding SaveQuotationCommand}" Background="#1677C8" Foreground="White" Margin="8,0,0,0"/></StackPanel></StackPanel></Grid></Border></Grid></Grid>
</UserControl>
